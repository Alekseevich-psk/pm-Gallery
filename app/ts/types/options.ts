export type Options = {
    elementForPreviews?: string,
    navigation?: {
        elBtnPrev: string,
        elBtnNext: string,
    },
    activeSlide?: number,
    pagination?: boolean,
    objectFit?: string,
    autoPlay?: number,
    fullScreenMode?: boolean,
    positionPreviews?: string,
    animationSlide?: string 
}