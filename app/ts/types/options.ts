export type Options = {
    elementForPreviews?: string,
    navigation?: {
        elBtnPrev: string,
        elBtnNext: string,
    },
    activeSlide?: number,
    pagination?: boolean,
    autoPlay?: number,
    fullScreenMode?: boolean,
    orientationPreviews?: string
}