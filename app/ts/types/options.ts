export type Options = {
    elForPreviews?: string,
    elForMainPicture?: string,
    navigation?: {
        elBtnPrev: string,
        elBtnNext: string,
    },
    activeSlide?: number,
    autoPlay?: number,
    fullScreenMode?: boolean,
    orientationPreviews?: string
}