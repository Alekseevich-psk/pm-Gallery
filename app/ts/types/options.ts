export type Options = {
    elementForPreviews?: string,
    navigation?: {
        elBtnPrev: string,
        elBtnNext: string,
    },
    activeSlide?: number,
    autoPlay?: number,
    fullScreenMode?: boolean,
    orientationPreviews?: string
}