export type initOptions = {
    countPreSlides?: number | string,
    positionPreviews?: string,
    visiblePreviews?: boolean,
    slideWidth?: number,
    slideHeight?: number,
    speedAnim?: number,
    breakpoints?: object,
    fullScreen?: object,
}