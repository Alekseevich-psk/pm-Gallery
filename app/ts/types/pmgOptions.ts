export type initOptions = {
    countPreSlides?: number | string,
    positionPreviews?: string,
    slideWidth?: number,
    slideHeight?: number,
    speedAnim?: number,
    breakpoints?: object,
    fullScreen?: object,
}