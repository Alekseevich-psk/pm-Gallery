import wheelScroll from "./wheel-scroll";
import sliderPreviews from "./slider-previews";

function scrollPreviews(pmGallery: any) {
    wheelScroll(pmGallery);
    sliderPreviews(pmGallery);
}

export default scrollPreviews;